# 燃点欠款项目可视化

一个浏览器端的 Excel 欠款分析工具。它读取欠款工作簿，将每个有效 Sheet 转成可切换账单，并提供总览、趋势、产品构成、明细账本和产品欠款排行。

## 快速开始

环境要求：Node.js 18+，推荐使用 pnpm。

```powershell
pnpm install
pnpm dev
```

打开终端输出的本地地址，通常是 `http://127.0.0.1:4173/`。生产构建：

```powershell
pnpm test
pnpm build
pnpm preview
```

## 使用方法

1. 页面启动时会自动加载内置账表，当前版本内置 `工作簿1.xlsx夏建强.xlsx`、`工作簿1.xlsx朱高云.xlsx` 和 `工作簿2.xlsx赵卫华.xlsx`。
2. 顶部导航顺序为“上传文件”“总览”“欠款排行”。上传文件页会实时显示当前账单，但不提供顶部账单选择器。
3. 点击“导入文件夹”，选择包含人名子文件夹的大文件夹，例如 `C:\Users\Mr.shen\Desktop\欠款整理`。程序会递归读取子文件夹中的 `.xlsx`、`.xls`、`.xlsm` 和 `.csv` 文件，并按每个表格的直接上级文件夹归类。
4. 文件夹卡片显示人名、表格解析出的公司名、账单数量、明细数量和金额。点击文件夹后，可以查看其中的表格；点击表格后可以选择“在 Excel 中打开”或“打开总览”。
5. 从上传文件打开账单总览后，左上角的“返回上传文件”会回到上传文件页面。重复导入会自动跳过相同内容，空 Sheet 会被忽略；“清除导入”只清除本次上传内容，内置账单会保留。

刷新页面会清空本次浏览器会话中临时追加的账单，并重新载入项目内置的账表。

## 本次更新

- 新增文件夹导入：一次选择父文件夹，自动读取人名子文件夹中的所有表格。
- 新增文件夹卡片和弹框：按人名归类，展示公司名及账单统计，表格支持 Excel 原文件和总览两种入口。
- 上传文件导航移到第一位，导入状态、清除导入和当前账单集中在上传文件页。
- 从总览返回上传文件时保留清晰的左上角返回入口；重复导入检测和临时文件链接释放逻辑同步完善。

## 使用效果

![上传文件夹总览](docs/screenshots/upload-library-folder-view.png)

![文件夹内表格弹框](docs/screenshots/upload-folder-dialog.png)

## Excel 数据规则

解析器兼容 PRD 约定格式，也兼容当前实际工作簿的行位置差异：

- 供应商名称：识别包含 `供货商` 的单元格。
- 欠款人：识别包含 `客户欠款` 的单元格。
- 年份：优先识别前 16 行内的独立年份单元格。
- 明细列：日期、产品名称、单位、数量、单价、欠款金额，对应 A-F 列。
- 空日期：继承上一条有效日期，保证同一批明细可以按日/月统计。
- 汇总行：识别 `月份金额`、`合计金额`、`余欠`、`付款`、`汇公账`、`税点` 等关键词，不计入当前明细合计。

页面中的“明细合计”只累加明细行的欠款金额；“原表计算明细”逐行展示 Excel 底部的原始汇总，包括月金额、上期余欠、税点、付款抵扣和最终余欠。最终余欠不会被重复加进明细合计，金额按原始小数精度展示。

## 构成逻辑

```text
Excel 文件 / 内置资产 / 用户多文件上传
                  |
                  v
       SheetJS workbook parser
                  |
                  v
    DebtLedger[]: 元数据、明细、汇总、余额
                  |
        +---------+---------+
        |                   |
        v                   v
  analytics.ts        React views
  总额/趋势/排行       总览 / 排行 / 表格
```

核心数据流：

1. `src/lib/workbook.ts` 读取工作簿，识别有效 Sheet 并生成统一的 `DebtLedger`。
2. `src/lib/analytics.ts` 从明细行派生总额、日/月趋势、产品聚合、排行和月份分组。
3. `src/App.tsx` 管理账单列表、当前账单、视图切换、默认账表加载和多文件上传。
4. `src/components/OverviewView.tsx` 展示总览指标、原表计算明细、趋势图、产品环图和 Accordion 明细表。
5. `src/components/LeaderboardView.tsx` 展示产品欠款排行，点击排行项展开对应明细。

## 目录结构

```text
src/
  assets/                 内置 Excel 工作簿
  components/             导航、总览、图表、账表、排行组件
  lib/
    workbook.ts           Excel 读取与规范化
    analytics.ts          统计与派生数据
    format.ts             金额、日期、百分比格式化
  App.tsx                 页面状态与数据流
  styles.css              响应式视觉系统
```

## 技术栈

- React + TypeScript + Vite
- SheetJS `xlsx`：浏览器端 Excel 解析
- Recharts：趋势图与产品环图
- Framer Motion：页面、数字和展开动画
- Lucide React：界面图标
- Vitest：解析、聚合和金额精度回归测试
